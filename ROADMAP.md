# 版本迭代规划（Roadmap）

本文档记录 AI 投资决策器后续版本的升级规划，便于跨会话跟踪进展。

---

## 版本总览

| 版本 | 状态 | 核心能力 |
|---|---|---|
| v0.1 | ✅ 已完成 | LangGraph 多 Agent 辩论原型（CLI） |
| v0.2 | ✅ 已完成 | RAG 知识库 + Web Search + 早停 + 流式输出（CLI） |
| **v1.0** | 🚧 进行中 | **Web UI（Streamlit）+ 追问对话 + 历史存档 + Gemini 2.5 Pro** |
| v2.0 | 📋 规划中 | 深度交互：指定大师追问、并行打字动画、中途插话、历史回看 |
| v3.0 | 💡 设想 | 生产级：Next.js + FastAPI、用户账户、云部署、A 股回测 |

---

## v1.0 · 当前版本交付范围

### UI 升级：Streamlit 单页 Web App
- 用户在浏览器输入问题 → 市场数据卡 → 5 大师按分层随机顺序流式发言（串行揭晓）→ 最终研报 Markdown 渲染
- 辩论结束后，页面底部出现追问对话框
- 侧边栏展示历史辩论，点击可回看

### 模型升级：Gemini 2.5 Pro 混合策略
- **推理**（大师辩论 + 研究员总结）：`gemini-2.5-pro`
- **搜索**（`web_search.py` 中的 Google Search grounding）：继续用 `gemini-2.5-flash`
- 原因：搜索不需要深度推理，Pro 纯属浪费 token 和时间

### 追问对话（D1 选项 1）
- 单一"综合助手"回答：带上完整研报 + 用户问题，由一个 LLM 统一作答
- 不做 @指定大师，避免 RAG 再检索和状态复杂化

### 辩论可视化（D2 选项 1）
- 沿用现有逻辑：分层随机顺序串行揭晓
- 用户视觉上看到一位接一位大师说话

### 辩论交互（D3 选项 1）
- 不支持中途插话
- 辩论开始后到结束前是"只读围观"

### 历史存档（D4 选项 2）
- SQLite 本地存储，文件路径 `data/history.db`
- 存表结构：`debates(id, created_at, query, final_report, raw_transcript_json)`
- 侧边栏按时间倒序列出；点击加载到主面板回看；支持基于历史辩论继续追问

---

## v2.0 · 下一版（用户已确认的方向）

### D1 选项 2：@指定大师追问 ⭐
- 用户输入 `@巴菲特 您觉得护城河体现在哪些方面？` → 由巴菲特 persona + 巴菲特 RAG 单独作答
- 实现：解析 `@xxx` → 路由到 `create_master_node`-like 的轻量节点 → 不进入辩论模式，单次响应
- 复用：原 persona prompt、RAG 检索逻辑、LLM 实例
- 工作量估算：~150 行

### D2 选项 3：真·串行发言 ⭐
- 取代目前的"并行 5 个节点 + UI 串行揭晓"
- 改为真正的串行 graph：Master_1 → Master_2 → ... → Master_5，后说的能看到本轮前说的
- 优点：符合真实辩论，大师间真正能互相反驳（不是伪辩论）
- 代价：单轮时长翻倍（从 ~40s 到 ~2 分钟/轮）
- 实现改动：重构 `main.py` 中的 graph 构建，从并行 fan-out 改为线性链 + 本轮倾向累积
- 工作量估算：~200 行 + graph 逻辑重构

### D3 选项 2：中途插话 ⭐
- 用户在辩论过程中有"插话"按钮
- 点击后弹出输入框，输入内容进入下一轮的上下文，所有大师能看到并在发言中回应
- 实现改动：
  - `AgentState` 增加 `user_interruptions: List[Dict]`
  - `create_master_node` 在组装消息时把 user interruption 作为"主持人追问"注入 system prompt
  - Streamlit UI 需要双向通道（考虑 `streamlit-feedback` 或改 FastAPI+WebSocket）
- 工作量估算：~250 行 + Streamlit → FastAPI 可能要迁移

### D4 选项 2 进阶：跨辩论上下文
- v1.0 已做 SQLite 历史保存
- v2.0 增加：用户问"我上次问的英伟达现在还适用吗？" → 系统能检索历史辩论并综合答复
- 可选：用 embedding 做历史辩论的语义检索（复用 BGE-M3）

---

## v2.0 实施顺序建议

1. **先做 D1-2（@指定大师追问）**：复用最多，风险最低，用户体验提升明显
2. **再做 D4 进阶（跨辩论上下文）**：30 行搞定，但效果拔群
3. **D2-3（真·串行辩论）与 D3-2（中途插话）绑定一起做**：因为都要改 graph + state，且都要 Streamlit → FastAPI 的迁移
4. 迁移到 FastAPI 后，顺带做 D3-2，同时前端可以升级成更好看的 React 或原生 HTML+SSE

---

## v3.0 · 远景（非承诺）

- **前端**：Next.js + shadcn/ui + Tailwind，暗黑模式、移动端适配
- **后端**：FastAPI + PostgreSQL + Redis（会话状态）
- **部署**：Vercel（前端）+ Railway/Fly.io（后端）
- **账户体系**：Google OAuth、免费额度 + Pro 付费
- **数据源升级**：接入实时行情（iFinD、同花顺、Tushare）、盘中数据刷新
- **能力扩展**：
  - **决策回测**：把大师的历史建议和真实行情对比，量化 Persona 的准确率
  - **对话式持仓管理**：用户上传组合 → 大师团队针对持仓做体检
  - **定期推送**：用户订阅标的，每周自动生成辩论报告

---

## 版本切换记录

| 版本 | 发布时间 | 备注 |
|---|---|---|
| v0.1 | 2026-04-10 | CLI 原型，无 RAG |
| v0.2 | 2026-04-17 | 加入 RAG + Web Search + 早停 |
| v1.0 | 2026-04-18 进行中 | Streamlit Web UI + 2.5 Pro |
