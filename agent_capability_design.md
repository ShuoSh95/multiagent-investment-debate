# 单个投资大师 Agent 能力建设完整方案

要让一个 LLM 真正拥有“大师级”的投资洞察力，单靠简单的 System Prompt（如“你是巴菲特”）是远远不够的。这会导致严重的“角色漂移”和“空话连篇”。

我们需要构建一个包含 **RAG (检索增强生成)**、**Tools/Skills (工具调用)** 和 **Advanced Prompting (高级提示词工程)** 的三位一体架构。以下是针对单个大师 Agent 的标准化建设方案。

---

## 1. 核心架构图

```text
                      +-------------------------------------------------+
                      |           Master Agent (e.g., GPT-4o)           |
                      +------------------------+------------------------+
                                               |
      +----------------------------------------+----------------------------------------+
      |                                        |                                        |
+-----v-----+                         +--------v--------+                      +--------v--------+
| 1. Memory |                         | 2. RAG System   |                      | 3. Agent Skills |
| (State)   |                         | (专属知识库)     |                      | (个性化分析能力) |
+-----------+                         +--------+--------+                      +--------+--------+
| - 辩论历史   |                         | - 大师著作       |                      | - 护城河打分模型  |
| - 当前标的   |                         | - 致股东信       |                      | - 宏观周期定位器  |
| - 市场现状   |                         | - 演讲实录       |                      | - DCF估值计算器   |
+-----------+                         +-----------------+                      +--------+--------+
                                               |
===============================================|=================================================
                                 【底层通用基建层 (Common Tools Base)】
                      +------------------------+------------------------+
                      | - get_financial_statements(ticker)              |
                      | - get_real_time_price(ticker)                   |
                      | - get_macro_indicators()                        |
                      | - web_search(query)                             |
                      | - python_code_interpreter()                     |
                      +-------------------------------------------------+
```

---

## 2. 模块一：RAG 知识库构建 (大师思想的灵魂)

RAG 是大师 Agent 的核心。必须保证它引用的每一句话、每一个逻辑都能在原著中找到映射。

### 2.1 数据源收集 (以巴菲特为例)
- **核心文本**：历年《致股东的信》（1965-至今）。
- **辅助文本**：《穷查理宝典》、历年伯克希尔股东大会 Q&A 实录（文本化）。
- **排除项**：绝对**不收录**任何第三方财经媒体对巴菲特的“解读”文章，防止信息被二手污染。

### 2.2 数据清洗与切片 (Chunking)
金融文本不能简单按字数切片（Recursive Character Text Splitter），否则会切断上下文逻辑。
- **语义切片**：采用基于标题/段落的切片方式（MarkdownHeaderTextSplitter），确保一个完整的投资案例（如 1988 年买入可口可乐的逻辑）在同一个 Chunk 内。
- **元数据 (Metadata) 标记**：为每个 Chunk 打上标签。
  - `year`: 1988
  - `topic`: [估值, 护城河, 消费品]
  - `company`: 可口可乐

### 2.3 向量化与检索策略 (Retrieval)
- **Embedding 模型**：选择擅长金融领域的 Embedding 模型（如 OpenAI `text-embedding-3-large` 或开源的 `BGE-M3`）。
- **混合检索 (Hybrid Search)**：
  - 纯向量检索可能找不到具体的数字（如“ROE > 15%”）。
  - 必须结合 **BM25 (关键词检索)** + **Vector Search (语义检索)**。
- **Self-Query 机制**：让 Agent 在检索前，先将用户的提问转化为大师自己的语言体系。
  - *用户提问*：“特斯拉现在能买吗？”
  - *Agent Self-Query*：“寻找关于高市盈率、科技硬件公司、商业模式护城河的论述。”

---

## 3. 模块二：底层通用 Tools 与 Agent 专属 Skills (基建与上层建筑)

为了避免重复开发，并将数据获取与数据分析解耦，我们采用 **“通用基建 (Tools) + 个性化技能 (Skills)”** 的分层架构。

### 3.1 底层通用基建 (Common Tools Base)
这些工具是整个系统的底层服务，**所有 Agent 共享使用**，本质上是封装好的外部 API 接口或执行环境：
1. **`get_financial_statements(ticker)`**：获取财报数据（三大表历史数据）。
2. **`get_real_time_price(ticker)`**：获取实时股价、PE、PB、市值。
3. **`get_macro_indicators()`**：获取 CPI、10年期美债收益率等宏观指标。
4. **`python_code_interpreter()`**：安全的 Python 沙盒执行环境，用于精确的数学计算。

### 3.2 大师专属技能 (Agent Skills)
Skills 是基于底层 Tools 组合而成的**个性化数据处理逻辑**。不同的 Agent 会被赋予不同的 Skills：
- **巴菲特 Agent** 独有：`dcf_valuation_calculator()` (调用财报Tool + Python Tool，执行现金流折现估值)。
- **达利欧 Agent** 独有：`macro_cycle_positioning()` (调用宏观指标Tool，输出当前处于经济周期的哪个阶段)。
- **格林布拉特 Agent** 独有：`magic_formula_scorer()` (调用财报Tool，精确计算标的的 ROIC 和 Earning Yield 并打分)。

---

## 4. 模块三：Prompt 工程设计 (大师的性格约束)

Prompt 是防止“角色漂移”的最后一道防线。一个合格的大师 System Prompt 应该包含以下几个部分，以巴菲特为例：

### 4.1 身份与世界观定义
> "你现在是沃伦·巴菲特。你的投资哲学建立在价值投资之上，你认为买股票就是买企业的一部分。你只看重自由现金流、高且稳定的 ROE，以及极深的商业护城河。你对短期宏观经济预测毫无兴趣，也极度厌恶没有利润、仅靠概念炒作的公司。"

### 4.2 思考框架 (Chain of Thought 模板)
强制要求 Agent 按照特定的大师框架思考：
> "在分析任何标的时，你必须严格按以下顺序思考：
> 1. **业务理解**：这家公司靠什么赚钱？它的产品我能看懂吗？
> 2. **护城河**：它有品牌优势、转换成本、网络效应还是成本优势？
> 3. **财务健康**：调用工具查看它的自由现金流和 ROE。
> 4. **估值**：现在的价格是否具有安全边际？"

### 4.3 辩论与妥协底线 (针对多 Agent 环境)
> "在辩论中，如果其他 Agent（如达利欧）用宏观经济向好来劝你买入一只市盈率高达 100 倍且没有护城河的股票，**你必须坚决反驳**，绝不能妥协。你宁可错过，也不做错。但如果标的符合你的标准，即使宏观环境差，你也敢于在别人恐惧时贪婪。"

---

## 5. 单 Agent 工作流执行范例

当输入问题：“探讨特斯拉的投资价值”时，单个巴菲特 Agent 的内部执行流如下：

1. **Tool Call**: 调用 `get_financial_statements("TSLA")` 和 `get_market_data("TSLA")`。
2. **Observation**: 发现 TSLA 汽车毛利率下滑，PE 为 45 倍。
3. **Self-Query RAG**: 搜索知识库中关于“高市盈率”、“汽车行业（重资产）”、“毛利率下滑”的论述。
4. **RAG Context**: 检索到巴菲特曾说“汽车行业是资本密集型，很难产生持续的高资本回报率”、“为增长支付过高价格是灾难”。
5. **Reasoning (CoT)**: 结合数据和原著逻辑进行推理。
6. **Output**: 生成一段极具巴菲特风格的辩论发言，并在第10轮时给出 `[投票: 看空]` 或 `[投票: 观望]`。

---
**总结**：通过 **RAG 注入思想**、**Tools 赋予感知**、**Prompt 锁定性格**，我们就能打造出一个形神兼备、不可妥协的“大师数字分身”。